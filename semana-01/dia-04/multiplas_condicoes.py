print("=== Múltiplas condições - CampOne ===")

faturamento = 12000
status = "inativa"

print("faturamento >= 5000:", faturamento >= 5000)
print('status == "ativa":', status == "ativa")
print(
    'faturamento >= 5000 and status == "ativa":',
    faturamento >= 5000 and status == "ativa",
)
